# railway.toml - Configuración Railway para CARCOSA
[build]
  dockerfilePath = "Dockerfile.railway"

[deploy]
  healthcheckPath = "/"
  healthcheckTimeout = 30
  restartPolicyType = "on_failure"
  restartPolicyMaxRetries = 3